#include <stdio.h>
int p(int n);
int main()
{
    int n;
    scanf("%d", &n);
    printf("%d", p(n));
    return 0;
}
int p(int n)
{
    if(n==1)return 1;
    if(n==2) return 1;
    return p(n-2)+p(n-1);
}
